<div class="row">
  <div class="col-sm">
    <h1 class="display-4">Lista utenti</h1>
  </div>
</div>

<p id="notice"><%= notice %></p>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Iniziali</th>
      <th>Titolo</th>
      <th>Nome</th>
      <th>Email</th>
      <th>Ruolo</th>
      <th>Incaricabile</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.initials %></td>
        <td><%= user.job_title %></td>
        <td><%= user.display_name %></td>
        <td><%= user.email %></td>
        <td><%= user.role %></td>
        <td><%= yes_icon user.appointeeable %></td>
        <td><%= link_to 'Dettaglio', user, class: "btn btn-outline-dark btn-sm" %></td>
        <td><%= link_to 'Modifica', edit_user_path(user), class: "btn btn-outline-dark btn-sm" %></td>
        <td><%= link_to 'Elimina', user, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div class="row margin-bottom-20">
  <div class="col">
    <%= link_to 'Nuovo Utente', new_user_path, class: "btn btn-primary" %>
  </div>
</div>
