<!-- Modal -->
<div class="modal fade" id="confirm-non-participation-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel2" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel2">Conferma non partecipazione</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="col-12">
            <p>Al momento risultano persone incaricate alla partecipazione o proposte pendenti.</p>
            <p>Scegliendo di non partecipare all'evento essi riceveranno una notifica dell'annullamento dell'evento.</p>
            <p>E' possibile allegare un messaggio alla e-mail.</p>
            <%= form_with scope: :invitation, url: cancel_participation_invitation_path(@invitation), method: :patch, html: { autocomplete: :off }, data: { remote: false }, format: :html do |f| %>
              <div class="form-group mt-3">
                <%= f.label :comment, "Messaggio" %>
                <%= f.text_area :comment, rows: 4, class: "form-control" %>
                <small class="form-text text-muted">Saranno visibili solamente al partecipante selezionato (o agli utenti del gruppo)</small>
              </div>

              <%= f.submit "Conferma non partecipazione", class: "btn btn-primary" %>
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Annulla</button>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
