<nav class="navbar navbar-expand navbar-dark bg-dark static-top">

  <%= link_to 'Inviti', dashboard_path, class: "navbar-brand mr-1 navbar-logo" %>

  <button class="btn btn-link btn-sm text-white order-1 order-sm-0" id="sidebarToggle" href="#">
    <i class="fas fa-bars"></i>
  </button>


  <div class="d-none d-md-inline-block ml-auto mr-0 mr-md-3 my-2 my-md-0">
    <span class="current-user"><%= current_user.display_name %></span>
  </div>

  <!-- Navbar -->
  <ul class="navbar-nav ml-auto ml-md-0 float-right">

    <% if ENV.fetch('user_mode'){ 'devise' }=='switchable' %>
      <li class="nav-item dropdown no-arrow mx-1">
        <a class="nav-link dropdown-toggle" href="#" id="messagesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-envelope fa-fw"></i>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="messagesDropdown">
          <a class="dropdown-item" href="#">Cambia utente corrente (funzionalità demo):</a>
          <div class="dropdown-divider"></div>
          <% User.all.each do |user| %>
            <%= link_to user.display_name, url_for(switch_to_user: user.id), class: 'dropdown-item' %>
          <% end %>
        </div>
      </li>
    <% end %>

    <li class="nav-item dropdown no-arrow">
      <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="fas fa-user-circle fa-fw"></i>
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
        <% if policy(:user).index? %>
          <%= link_to "Utenti", users_path, class: "dropdown-item" %>
        <% end %>
        <%= link_to current_user.display_name, current_user, class: 'dropdown-item' %>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">Esci</a>
      </div>
    </li>
  </ul>

</nav>
