<div class="row">
  <div class="col-sm">
    <h1 class="display-5">Utilizzo web app / interazione utenti</h1>
  </div>
</div>

<p id="notice"><%= notice %></p>

<%= render 'few_months_shades' %>
<%= render 'one_day_shades', ui: @ui_day, day: @today, max_val: @ui_day_max %>

<%= render 'table', user_interactions: @user_interactions %>
